[project]
name = "digits"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.5.2",
    "duckdb-cli>=1.5.2",
    "pandas>=3.0.3",
    "pyarrow>=24.0.0",
    "pytest>=9.0.3",
]
